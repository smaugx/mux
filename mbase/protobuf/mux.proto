syntax = "proto3";

package mux.protobuf;

// the final message from/to socket
message MuxMessage {
   uint32   version                             = 1;  // message version
   uint32   type                                = 2;  // message type
   string   data                                = 3;  // the real binary content
}
